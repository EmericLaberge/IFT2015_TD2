public class App {
    public static void main(String[] args) throws Exception {
        // Vous pouvez tester votre code ici!
        Node linked = new Node(0, null);
        linked.addValue(1);
        System.out.println(linked.value.toString());
    }
}
